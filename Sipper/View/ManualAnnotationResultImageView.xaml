<Window x:Class="Sipper.View.ManualAnnotationResultImageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ManualAnnotationResultImageView" Height="429" Width="714" Name="window">
    <Grid>
        <ListView Margin="6,6,6,0" ItemsSource="{Binding Path=DataContext.Results, ElementName=window, Mode= TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="Key" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBox Width="100" Grid.Column="0" Text="{Binding Path=Result.TargetID}" />
                        <Image Grid.Column="1" Source="{Binding Path=ChromImageFilePath}"  />
                        <Image Grid.Column="2" Source="{Binding Path=TheorMSImageFilePath}"  />
                        <Image Grid.Column="3" Source="{Binding Path=MSImageFilePath}"  />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListView>
    </Grid>
</Window>
